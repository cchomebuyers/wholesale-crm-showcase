# Shell launchers must stay LF or macOS rejects them (bad interpreter: /bin/bash^M)
*.command text eol=lf
*.sh      text eol=lf
# Windows batch launchers need CRLF
*.cmd     text eol=crlf
*.bat     text eol=crlf
